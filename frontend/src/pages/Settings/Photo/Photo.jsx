import PhotoSettingsForm from '@/components/forms/Settings/Photo';

const PhotoSettingsPage = () => {
  return (
    <section className="min-h-screen h-full w-full flex justify-center items-center overflow-hidden">
      <PhotoSettingsForm />
    </section>
  );
};

export default PhotoSettingsPage;
