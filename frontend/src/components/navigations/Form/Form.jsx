import './Form.css';
import Navigation from '../../core/Navigation/Navigation';

const FormNavigation = (props) => {
  return <Navigation {...props} className="form__navigation" />;
};

export default FormNavigation;
